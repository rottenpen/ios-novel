# Yuedu 分页阅读与公开仓库整理 SPEC

状态：已完成。审查日期：2026-09-14。

## 1. 背景

Yuedu 提供基于书源的小说阅读功能。本规格定义横向逐页阅读、章内进度保存，以及公开源码所需的文档、许可和构建配置。

正文按实际屏幕区域分页。网络、脚本接口和搜索状态处理须满足下述安全性与一致性要求。实现与验证结果见 [代码审查报告](CODE_REVIEW.md)。

## 2. 目标

- 正文支持横向滑动、点击左右区域翻页；章末与下一章连续衔接。
- 退出、重新打开和调整排版后，恢复到原来的正文位置。
- 公开文件围绕当前功能、构建方法和维护方式组织，第三方来源与许可清晰可查。
- 修复已确认的跨站认证信息传递、脚本接口覆盖和搜索状态竞态，并补充行为测试。
- 形成可从干净目录构建、测试的本地 Git 仓库。

## 3. 用户故事与验收标准

### Story 1：逐页阅读

作为读者，希望每次翻动一页正文，以便持续横向阅读。

- [x] 打开书籍默认显示一屏正文，横向滑动翻到相邻页。
- [x] 点击左侧上一页、右侧下一页、中间呼出或隐藏工具栏。
- [x] 最后一页可进入下一章，第一页可返回上一章最后一页；全书首尾有明确边界。
- [x] 当前页显示章节标题与章内页码，保留目录、主题、字号、行距、边距、常亮设置。
- [x] 字号 14–30、行距 2–20、边距 10–44，以及横竖屏切换时，无文字截断、重叠、重复或遗漏。
- [x] 保留正文复制能力和辅助功能朗读，翻页操作有可访问的名称。
- [x] 空章节、加载失败、快速连续翻页都有明确状态，支持重试。

### Story 2：继续阅读

作为读者，希望再次打开书籍时继续读刚才的位置。

- [x] 同时保存章节索引和章内文字偏移；重新进入恢复到包含该偏移的页面。
- [x] 字号、行距、边距或屏幕尺寸变化后，重新分页仍围绕原来的文字位置定位。
- [x] 目录跳转从所选章节首页开始；“继续阅读”恢复已保存的位置。
- [x] 从新书详情直接点击目录也会保存阅读进度；书架查到更新后能打开最新目录。
- [x] 翻页、退出阅读器和应用进入后台时更新进度。
- [x] 现有书架、书源、缓存文件和排版偏好继续可用。
- [x] 对已有目录和正文缓存，书源缺失时仍能打开已缓存的内容。

### Story 3：公开维护

作为维护者，希望仓库能清楚介绍功能，并能被其他人拉取和构建。

- [x] 保留产品名“阅读 / Yuedu”；内部包、模块和测试统一使用 `ReaderCore`。
- [x] 应用图标采用定稿的“小书精灵”，提供无透明像素的 1024 × 1024 iOS 图标。
- [x] README 介绍实际功能、环境要求、模拟器与真机构建、书源导入、测试及能力边界。
- [x] 应用文案和自有代码注释聚焦当前行为；格式兼容说明使用准确、必要的名称。
- [x] 根目录提供 GPLv3 许可证；`THIRD_PARTY_NOTICES.md` 记录代码和测试数据来源、版本及适用许可；保留第三方版权声明。
- [x] Android 参考仓库备份至工作区外；SwiftSoup 保留当前源码和 MIT 许可，其嵌套 Git 元数据备份至工作区外。
- [x] `.gitignore` 排除构建产物、SPM 缓存、Xcode 用户状态、签名材料与本地配置。
- [x] 公开工程采用通用 bundle ID 默认值，开发团队和设备签名通过环境变量或构建参数提供。
- [x] 工程生成脚本连续运行不产生无意义的对象 ID 变化。
- [x] 本地 Git 的待提交清单完整包含应用、核心库、依赖源码、测试和许可文件，不包含嵌套 gitlink 或个人构建资料。

### Story 4：可靠的网络与脚本执行

- [x] 跨 origin 重定向不携带原站点的 Authorization、Cookie 等敏感头；HTTPS 降级有明确处理。
- [x] 303 按重定向语义切换请求；307/308 保持请求方法和正文；301/302 的兼容处理有明确范围和测试。
- [x] Cookie 按有效域名、路径、Secure 和有效期选择，不因简化主域名归组而跨站共享。
- [x] `java.get(key)` 与 `java.get(url, headers)` 分别执行变量读取和网络读取。
- [x] `cookie` 与 `cache` 有独立的宿主对象；缓存读写、有效期及已支持的 Cookie 接口可用。
- [x] 数组、字典和空值传入 JavaScript 时保留数据结构，属性与元素访问结果正确。
- [x] 搜索 A 被搜索 B 替代后，A 的结束不能覆盖 B 的结果、统计和加载状态。
- [x] 关键失败有中文日志，日志不输出认证头、Cookie 或完整敏感请求参数。

## 4. 技术方案

### 4.1 分页与阅读状态

采用按实际排版尺寸计算的页面范围，排版与展示使用一致的字体、段落样式和可用区域。优先使用系统 TextKit / CoreText 排版能力；避免按固定字数估算页数。

页面用章节索引和正文 UTF-16 范围标识。阅读锚点为“章节索引 + 正文 UTF-16 偏移”，复用 `Book.durChapterPos` 保存文字位置。旧数据中的位置 0 对应章节开头，保持已有 JSON 字段不变。

阅读器负责当前页、目录跳转和工具栏；分页组件负责文本布局；横向页面容器负责滑动与点击翻页。只保留当前章节及相邻章节的必要页面与加载任务，避免为全书创建完整视图和排版缓存。相邻章节预加载失败不阻断当前页阅读。

切换排版时先记录文字锚点，再重算页面并定位。异步加载以章节身份和请求代次校验，取消或过期结果不能改变当前阅读位置。

### 4.2 网络与搜索

重定向以当前请求和响应状态码计算后续请求，不无条件回填初始请求的所有头。Cookie 使用包含域、路径、安全标记和有效期的记录，并在目标 URL 上重新选择。

JavaScript 桥按参数签名区分变量读取与网络读取，为缓存和 Cookie 分别安装对象。保持既有书源格式与变量语义，并用本地 HTTP 服务验证网络调用。

每轮搜索有独立身份。合并结果、更新统计和结束加载均须确认仍属于当前轮次，并正确传播取消。

### 4.3 命名、来源与仓库

核心包、导入路径、工程生成器和测试统一使用 `ReaderCore`。书源 JSON 的协议字段、用户数据文件名和 UserDefaults 键保持兼容。

公开代码按 GPLv3 分发。第三方版本、作者、许可及修改说明集中记录在 `THIRD_PARTY_NOTICES.md`，并保留相关文件声明。

根目录文档围绕当前项目组织。第三方代码保留其原始声明。内置样本与社区书源按第三方内容说明，可用性以站点当前状态为准。

公开工程使用通用签名配置；真机复验沿用当前设备安装的 bundle ID 和本地签名参数，以保持应用数据连续。

## 5. 文件与实施顺序

| 顺序 | 文件或组件 | 工作 |
| --- | --- | --- |
| 1 | 核心网络、脚本、搜索及对应测试 | 补充本地行为测试，修复已复现问题 |
| 2 | `ReaderView`、分页组件、`BookDetailView`、书架进度 | 实现逐页阅读、位置恢复和缓存入口 |
| 3 | 核心包目录、`Package.swift`、App 导入、工程生成器 | 统一 `ReaderCore` 命名，检查数据兼容 |
| 4 | README、许可证、第三方声明、`.gitignore` | 整理公开仓库内容与依赖来源 |
| 5 | 参考仓库、依赖 Git 元数据、本地 Git | 工作区外备份，建立可完整提交的目录结构 |
| 6 | 单元测试、构建、模拟器与真机 | 复验功能与发布准备状态，更新审查结论 |

## 6. 验证计划

| 范围 | 验证内容 |
| --- | --- |
| 分页算法 | 长章节、空内容、单页、中文标点、emoji、组合字符；所有页面连续覆盖正文且可重建原文 |
| 阅读进度 | 同章恢复、跨章首尾、目录跳转、横竖屏和排版变化、后台保存、旧数据位置 0 |
| 异步与缓存 | 快速翻页、过期加载、缺失书源但缓存存在、预加载失败及重试 |
| 网络 | 本地服务覆盖 301/302/303/307/308、跨 origin 认证头、Cookie 域/路径/安全属性/过期 |
| 脚本 | 变量读取与 HTTP GET 重载、独立 Cookie/缓存接口、缓存有效期、结构化输入 |
| 搜索 | 连续启动两轮、取消、旧任务晚结束、空输入及并发参数边界 |
| 仓库 | 从干净副本构建和测试；重复生成工程；检查暂存清单、嵌套仓库和敏感数据模式 |
| 真机 | 覆盖安装后确认已有书架可读，检查连续翻页、跨章、恢复和文字显示 |

## 7. 依赖与风险

- 继续使用 SwiftUI、系统文字排版能力和当前版本 SwiftSoup。
- 排版计算与实际绘制必须一致，否则可能出现页末截字；以长文和极端排版设置验收。
- Cookie 和重定向修复可能改变依赖宽松网络行为的书源，应以明确协议规则和本地回归用例验证。
- 第三方书源可用性随站点变化；测试数据的解析通过不能等同于线上长期可用。
- 参考仓库和 Git 元数据先完整备份，再进行目录整理。

## 8. 当前进度

- [x] 完成阅读链路、核心网络、脚本桥、搜索状态、仓库与许可审查。
- [x] 运行现有 170 个测试并通过；对关键问题完成独立复现。
- [x] 确认本 SPEC。
- [x] 实施修改并完成验证计划。
