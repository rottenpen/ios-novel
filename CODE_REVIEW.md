# 代码审查与验证

审查范围：阅读界面、规则执行、网络、搜索、存储与公开仓库配置。更新日期：2026-09-14。

## 实现结果

| 范围 | 当前行为 | 验证 |
| --- | --- | --- |
| 分页阅读 | CoreText 按实际尺寸分页，同一排版结果用于绘制；横向滑动、左右点击、跨章首尾衔接 | 分页范围连续覆盖正文；中文、标点、emoji、组合字符、长段落和多组排版测试 |
| 阅读位置 | 保存章节索引和 UTF-16 偏移，重新排版定位到包含原位置的页面 | 跨章、恢复、重排和越界测试 |
| 加载状态 | 仅保留当前及相邻章节正文，过期结果不能改变当前页面，失败可重试 | 快速目录跳转、取消、缓存上限和重试测试 |
| 阅读入口 | 直接点击目录与继续阅读统一入架和进度保存；缓存目录可在书源缺失时打开 | 新书入架、偏移保存、重新加载及缓存读取测试 |
| 目录更新 | 书架刷新同时保存最新目录与章节数 | 目录缓存更新后立即读取新结果 |
| 重定向 | 每次跳转检查 origin，跨站清除认证及自定义敏感头；阻止 HTTPS 降为 HTTP | 本地 HTTP 服务覆盖 301/302/303/307/308，同站与跨站请求方法、正文和请求头；多跳与降级策略测试 |
| Cookie | 使用独立 Cookie 存储，按域、路径、安全属性和有效期选择 | 子域、公有后缀、Path、Secure、过期删除及重定向响应 Cookie 测试 |
| JavaScript | 区分 `java.get(key)` 与 `java.get(url, headers)`，独立安装 `cache` / `cookie` 对象 | 本地网络 GET、变量、数组、字典、空值、缓存过期测试 |
| 搜索 | 每轮搜索独立标识，旧任务不能结束新搜索或覆盖统计 | 连续搜索、空输入和并发参数边界测试 |
| 图标 | 使用“小书精灵”定稿，提供不透明的 1024 × 1024 PNG | 资源尺寸、透明通道、构建产物图标配置检查 |
| 仓库 | 核心包统一为 ReaderCore；忽略个人状态和构建产物；依赖源码完整纳入仓库 | 确定性工程生成、暂存清单、gitlink、常见密钥模式和干净源码构建检查 |

301/302 在同 origin 内保留请求方法和正文以兼容书源；跨 origin 使用系统重定向建议。303 除 HEAD 外改为 GET；307/308 保留方法和正文。跨站 Cookie 由目标地址重新选择，不恢复原始请求的显式 Cookie 头。

## 验证记录

- `swift test --package-path ReaderCore`：184 个测试、17 个测试套件通过。分页参数用例覆盖字号 14/19/30、行距 2/9/20，以及窄屏和横屏尺寸。
- 模拟器实际验证了点击前后翻页、横向手势、竖向手势不滚动、中间阅读菜单和正文显示；辅助功能树提供正文、页码及上一页/下一页动作。
- Xcode 26.6 真机 Debug 构建通过，包含图标资源的版本已覆盖安装并启动。设备仍使用原 bundle ID，用户反馈基本阅读功能可用。
- 工程生成器连续运行时 `project.pbxproj` 内容完全一致；公开工程不包含个人开发团队。
- 暂存清单不含嵌套 gitlink、构建目录、Xcode 用户状态或签名材料。常见密钥模式在自有文件中无命中；第三方 HTML 测试语料中的匹配项为公开新闻链接片段。
- 自有文件的 `git diff --cached --check` 通过；SwiftSoup 保留上游源码格式和许可。
- 从 Git 暂存内容导出的干净源码副本：184 个测试、17 个套件通过；Release 模拟器构建（arm64 / x86_64）通过。

## 能力边界

- 书源执行 JavaScript、访问第三方网站；依赖 WebView 验证、字体解码、压缩包或远程脚本导入的规则不在当前支持范围。正文图片显示为文字标记。
- 书源格式兼容测试通过不代表站点持续可用；`LiveCheck` 的结果受实际网络和第三方站点影响。
- 普通 HTTP 书源仍可使用；HTTPS 到 HTTP 的重定向会被拒绝。需要宽松认证头转发的书源应调整规则。
- 第三方来源和许可证见 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)。
